# Self-hosting

*Stub — populated in Phase 8.*

Covers: Docker compose, Kubernetes Helm chart, backups (DB + encryption-key rotation), scaling workers, leader-election operations, log aggregation, metrics scraping.

See `IMPLEMENTATION_BIBLE.md §5` (process layout) and `§19` (observability).
