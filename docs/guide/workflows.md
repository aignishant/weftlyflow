# Workflows

*Stub — populated in Phase 1.*

Covers: creating, editing, versioning (`workflow_history`), activating / deactivating, sharing (project-scoped RBAC), exporting/importing JSON, pinning data for tests.
