{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Personal overrides — copy to settings.local.json (gitignored). Merges on top of settings.json.",
  "permissions": {
    "allow": []
  },
  "env": {
  }
}
