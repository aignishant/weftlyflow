# Slash commands

Kept for backward compatibility; skills are preferred for new workflows.

| Command | Purpose |
|---|---|
| `/plan <goal>` | Draft an implementation plan (approval-ready, no code written). |
| `/research <topic>` | Parallel deep research via subagents. |
| `/git:commit` | Draft a Conventional Commits message for staged changes. |
